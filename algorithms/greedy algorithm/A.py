def orderStones(n_orders, n_weight, m_delivered, m_weight):
    """
    https://contest.yandex.ru/contest/53748/problems/A/
    A. Камни с Марса

    При отправке марсианской миссии множество научных лабораторий и музеев сделали заказы на доставку образцов марсианских горных пород.
    В каждом заказе обозначен минимальный вес требуемого образца: заказчик готов получить образец указанного веса или тяжелее, 
    но никак не меньше.
    Марсианские булыжники были доставлены на Землю, но ни их количество, ни вес не соответствуют заказам в точности:
    8 5 5 8 6 9 8 2 4 7   # Заказано 10 образцов;
                        # каждый элемент - вес заказанного образца.
    9 8 1 1 1 5 10 8      # Доставлено 8 образцов;
                        # каждый элемент - вес доставленного образца.
    Ваша задача: написать программу, которая распределит образцы между заказчиками оптимальным образом — так, 
    чтобы выполнить требования максимального числа заказчиков.
    Требование заказчика считается выполненным, если он получит образец, вес которого равен заказанному или превышает заказанный вес.
    Программа должна вернуть число заказчиков, которые получили образцы, соответствующие их требованиям.
    8 5 5 8 6 9 8 2 4 7   # Заказано.
    9 8 1 1 1 5 10 8      # Доставлено.
    8 5 5 8  6 9 8 2 4 7  # Доставленные образцы, распределённые между заказчиками
    | | | |  |            # (один из возможных вариантов).
    9 8 5 10 8            # Три образца весом 1 кг остались невостребованными.
    # Результат (число заказчиков, получивших желаемое):
    5
    Булыжники нельзя дробить на части, а каждый заказчик может получить только по одному образцу.

    Формат ввода
    В первой строке записано число n — количество заказов от музеев и лабораторий. Это количество не превышает 10 000.
    Во второй строке — n чисел, разделённых пробелом. Каждое число — это минимальный вес образца, который требуется заказчику. 
    Это натуральные числа, не превосходящие 1000.
    В третьей строке записано число m — количество образцов, доставленных с Марса. Это количество не превышает 10 000.
    В четвёртой строке — m чисел: это вес каждого из доставленных образцов; значения в этой строке не превышают 1000.

    Формат вывода
    Нужно вывести одно число — количество заказчиков, требования которых удовлетворены.

    Пример 1
    Ввод

    10
    8 5 5 8 6 9 8 2 4 7
    8
    9 8 1 1 1 5 10 8
    Вывод
    5

    Пример 2
    Ввод
    10
    8 2 4 7 8 5 5 8 6 9
    13
    9 8 1 1 1 5 10 8 2 7 4 3 15
    Вывод
    9
    """
    # Сортируем заказы по возрастанию минимального веса
    n_weight = sorted(n_weight)
    # Сортируем доставленные образцы по возрастанию веса
    m_weight = sorted(m_weight)
    
    count = 0
    i = 0  # указатель на заказы
    j = 0  # указатель на доставленные образцы
    
    # Проходим по всем заказам и образцам
    while i < n_orders and j < m_delivered:
        # Если текущий образец удовлетворяет текущему заказу
        if m_weight[j] >= n_weight[i]:
            count += 1  # удовлетворяем заказ
            i += 1      # переходим к следующему заказу
            j += 1      # переходим к следующему образцу
        else:
            # Образец слишком мал для текущего заказа, пробуем следующий образец
            j += 1
    
    return count

def main():
    n_orders = int(input())
    n_weight = list(map(int, input().split()))
    m_delivered = int(input())
    m_weight = list(map(int, input().split()))
    print(orderStones(n_orders, n_weight, m_delivered, m_weight))

if __name__ == '__main__':
    main()